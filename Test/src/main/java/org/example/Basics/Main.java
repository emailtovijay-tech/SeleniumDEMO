package org.example.Basics;


public class Main {
    public static void main(String[] args) throws InterruptedException {

/*
    WebDriver driver = new ChromeDriver(); // Initializing the Chrome browser
    driver.get("https://www.shadi.com/"); // Hitting the URL
    driver.navigate().to("https://www.shadi.com/"); // Navigating to URL
    driver.manage().window().maximize(); // Maximizing the browser window
    driver.close();
    driver.findElement(By.id("prev2")).click();  // ID locator
    List <WebElement> link = driver.findElements(By.className("nav-item"));  // Class Name Locator
    System.out.println("Total number of link " + link.size());  // Getting total number of link on Web page
    List <WebElement> Images = driver.findElements(By.tagName("img"));   // Tag Name Locator
    System.out.println("Total number of images " + Images.size());     //  Getting total number of Images on Web page
    driver.findElement(By.linkText("Phones")).click();      // Link Text Locator
    driver.findElement(By.partialLinkText("Laptop")).click(); // Partial link text Locator
    driver.findElement(By.cssSelector("input#small-searchterms")).sendKeys("tag#ID"); // Tag#ID combination in CSS selector locator
    driver.findElement(By.cssSelector("#small-searchterms")).sendKeys("#ID"); // #ID only without Tag in CSS selector locator
    driver.findElement(By.cssSelector("input.search-box-text")).sendKeys("Tag.Classname"); // Tag.classname combination in CSS selector locator
    driver.findElement(By.cssSelector(".search-box-text")).sendKeys(".Classname"); // ".classname" in CSS selector locator
    driver.findElement(By.cssSelector("input[placeholder='Search store']")).sendKeys("Classname"); // "Tag[attribute = "value"]" combination in CSS selector locator
    driver.findElement(By.cssSelector("input.search-box-text[placeholder = 'Search store']")).sendKeys("Classname"); // "Tag.Classname[attribute = "value"]" combination in CSS selector locator
    driver.navigate().back(); // To navigate backward
    driver.navigate().forward(); // To navigate forward
    driver.navigate().refresh(); // To Refresh the page

*/
    }
}
